import SwiftUI

@main
struct QuotaFloatSigningHostApp: App {
    var body: some Scene {
        Settings {
            Text("Quota Float Widget signing host")
                .padding()
        }
    }
}
